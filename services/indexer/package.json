{
  "name": "xny-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts'",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ xny-indexer",
    "create-local": "graph create --node http://localhost:8020/ xny-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ xny-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xny-indexer",
    "test": "graph test -d"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
